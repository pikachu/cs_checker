<div class="container">
  <h3>Signup Form</h3>
  {{#if messages.success}}
    <div class="text-success">
      {{#each messages.success}}
        <div>{{msg}}</div>
      {{/each}}
    </div>
  {{/if}}
  {{#if messages.error}}
    <div class="text-danger">
      {{#each messages.error}}
        <div>{{msg}}</div>
      {{/each}}
    </div>
  {{/if}}
  <form method="POST">
    <label for="email">Email</label>
    <input type="email" name="email" id="email" autofocus>
    <label for="password">Password</label>
    <input type="password" name="password" id="password">
    <label for="umdusername">University User Name</label>
    <input type="text" name="umdusername" id="umdusername" autofocus>
    <label for="umdpass">University Password</label>
    <input type="password" name="umdpass" id="umdpass">
    <label for="phoneNumber">Phone Number</label>
    <input type="text" name="phoneNumber" id="phoneNumber">
    <label for="courses">Courses</label>
    <textarea name="courses" id="courses" rows="7"></textarea>
    <br>
    <button type="submit">Send</button>
  </form>
</div>
